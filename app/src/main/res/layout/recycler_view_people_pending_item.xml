<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/recyclerViewPeoplePendingLayout"
    android:layout_width="match_parent"
    android:layout_height="100dp">

    <TextView
        android:id="@+id/personNameText"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintRight_toLeftOf="@id/deletePerson"
        app:layout_constraintHorizontal_chainStyle="spread_inside"
        android:gravity="center|left"
        android:text="People"
        android:textColor="@android:color/primary_text_light_nodisable"
       />

    <Button
        android:id="@+id/deletePerson"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintLeft_toRightOf="@id/personNameText"
        app:layout_constraintRight_toRightOf="parent"

        android:background="@drawable/delete_person_button"
        android:text="Done"
        />
</android.support.constraint.ConstraintLayout>